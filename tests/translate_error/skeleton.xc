#include <skeleton.xh>
#include <stdio.h>

int main (int argc, char **argv) {

  printf ("Testing our silly Skeleton extension.\n") ;

  !skeleton ;  // Syntax from the base extension, which isn't included in this composed language.

  return 0 ; 
}
